#include <stdio.h>

int main() {
    int x = 8;  // 1000 in binary
    printf("x << 1 = %d\n", x << 1); // Left shift = 16
    printf("x >> 1 = %d\n", x >> 1); // Right shift = 4
    return 0;
}

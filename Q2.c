#include <stdio.h>
#include <math.h>

int main() {
    double x = 2.0;
    printf("sin(2) = %.2f\n", sin(x));
    printf("cos(2) = %.2f\n", cos(x));
    printf("log(2) = %.2f\n", log(x));
    printf("pow(2,3) = %.2f\n", pow(x, 3));
    printf("sqrt(2) = %.2f\n", sqrt(x));
    return 0;
}